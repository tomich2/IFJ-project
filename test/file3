var
   { local variable definition }
   a : integer;
begin
   a := 100;
   if( a < 20 ) then
     begin
       write('a is less than 20'#10'' )
     end
   else 
     begin
       write('a is not less than 20'#10'' )
     end;
    write('value of a is : '#10'', a)
end.
