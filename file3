var
   { local variable definition }
   a : integer;
begin
   a := 100;
   if( a < 20 ) then
       write('a is less than 20' )
   else 
       write('a is not less than 20' );
    write('value of a is : ', a);
end.
